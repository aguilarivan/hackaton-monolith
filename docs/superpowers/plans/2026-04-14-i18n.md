# i18n (Spanish + English Toggle) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add English as a second language to Day Zero with a header toggle, URL-prefix routing via `next-intl`, and locale-aware AI content generation.

**Architecture:** All pages move under `app/[locale]/`. Middleware handles locale detection. Translation JSON files hold all UI strings. AI prompts receive locale to generate content in the correct language. A header toggle switches between `/es` (default, no prefix) and `/en`.

**Tech Stack:** next-intl, Next.js 16 App Router, TypeScript, React 19

---

## File Structure

### New files
| File | Responsibility |
|------|---------------|
| `i18n/config.ts` | Locale constants and types |
| `i18n/request.ts` | `next-intl` server-side message loading |
| `i18n/routing.ts` | Shared routing config for `next-intl` |
| `i18n/navigation.ts` | Re-exports locale-aware `Link`, `useRouter`, `usePathname`, `redirect` |
| `middleware.ts` | Locale detection and routing middleware |
| `messages/es.json` | Spanish translation strings |
| `messages/en.json` | English translation strings |
| `components/language-toggle.tsx` | Language switch button |
| `app/[locale]/layout.tsx` | Locale-aware layout (moved from `app/layout.tsx`) |
| `app/[locale]/page.tsx` | Home page (moved) |
| `app/[locale]/login/page.tsx` | Login page (moved) |
| `app/[locale]/preguntas-claude/page.tsx` | Questions page (moved) |
| `app/[locale]/analisis/page.tsx` | Analysis page (moved) |

### Modified files
| File | Change |
|------|--------|
| `app/layout.tsx` | Becomes a minimal root layout (no `<html>` tag — that moves to `[locale]/layout.tsx`) |
| `next.config.mjs` | Add `next-intl` plugin |
| `components/header.tsx` | Use `useTranslations()`, add language toggle, use `next-intl` navigation |
| `components/hero-input.tsx` | Use `useTranslations()`, locale-aware number formatting |
| `components/startup-dashboard.tsx` | Use `useTranslations()`, locale-aware formatting |
| `components/dashboard/*.tsx` (10 files) | Use `useTranslations()` for all labels |
| `lib/flow-api.ts` | Pass `X-Locale` header on requests |
| `lib/server/flow-store.ts` | Add `locale` field to `FlowRecord` |
| `lib/server/questions-prompt.ts` | Accept locale, switch system prompt language |
| `lib/server/section-plan-prompt.ts` | Accept locale, switch system prompt language |
| `app/api/analysis/route.ts` | Read `X-Locale` header, pass to generators |
| `app/api/questions/route.ts` | Read `X-Locale` header, pass to prompt builder |
| `app/api/section-plan/route.ts` | Read `X-Locale` header, pass to prompt builder |
| `app/api/flow/route.ts` | Store locale on flow creation |

---

## Task 1: Install next-intl and set up config files

**Files:**
- Modify: `package.json` (via pnpm)
- Create: `i18n/config.ts`
- Create: `i18n/routing.ts`
- Create: `i18n/request.ts`
- Create: `i18n/navigation.ts`
- Modify: `next.config.mjs`

- [ ] **Step 1: Install next-intl**

```bash
pnpm add next-intl
```

- [ ] **Step 2: Create `i18n/config.ts`**

```ts
export const locales = ["es", "en"] as const;
export type Locale = (typeof locales)[number];
export const defaultLocale: Locale = "es";
```

- [ ] **Step 3: Create `i18n/routing.ts`**

```ts
import { defineRouting } from "next-intl/routing";
import { locales, defaultLocale } from "./config";

export const routing = defineRouting({
  locales,
  defaultLocale,
  localePrefix: "as-needed", // no prefix for default locale (es)
});
```

- [ ] **Step 4: Create `i18n/navigation.ts`**

```ts
import { createNavigation } from "next-intl/navigation";
import { routing } from "./routing";

export const { Link, redirect, usePathname, useRouter, getPathname } =
  createNavigation(routing);
```

- [ ] **Step 5: Create `i18n/request.ts`**

```ts
import { getRequestConfig } from "next-intl/server";
import { routing } from "./routing";

export default getRequestConfig(async ({ requestLocale }) => {
  let locale = await requestLocale;

  if (!locale || !routing.locales.includes(locale as any)) {
    locale = routing.defaultLocale;
  }

  return {
    locale,
    messages: (await import(`../messages/${locale}.json`)).default,
  };
});
```

- [ ] **Step 6: Update `next.config.mjs`**

Replace the entire file:

```mjs
import createNextIntlPlugin from "next-intl/plugin";

const withNextIntl = createNextIntlPlugin("./i18n/request.ts");

/** @type {import('next').NextConfig} */
const nextConfig = {
  typescript: {
    ignoreBuildErrors: true,
  },
  images: {
    unoptimized: true,
  },
};

export default withNextIntl(nextConfig);
```

- [ ] **Step 7: Commit**

```bash
git add i18n/ next.config.mjs package.json pnpm-lock.yaml
git commit -m "feat(i18n): install next-intl and create config files"
```

---

## Task 2: Create translation JSON files

**Files:**
- Create: `messages/es.json`
- Create: `messages/en.json`

- [ ] **Step 1: Create `messages/es.json`**

Extract every hardcoded Spanish string from the codebase into a namespaced JSON file. This is the source of truth.

```json
{
  "common": {
    "newIdea": "Nueva idea",
    "login": "Ingresar",
    "logout": "Salir",
    "toggleTheme": "Cambiar tema",
    "backToStart": "Volver al inicio",
    "brandName": "DayZero",
    "tagline": "Tu copiloto de lanzamiento"
  },
  "metadata": {
    "title": "DayZero — Tu copiloto de lanzamiento",
    "description": "Validá tu idea de negocio con IA. Análisis de viabilidad, competencia, kit de arranque y hoja de ruta personalizada para Argentina."
  },
  "home": {
    "heroTitle1": "Lanzá el negocio",
    "heroTitle2": "que siempre soñaste",
    "heroDescription": "Describí tu idea, tu ciudad y tu inversión inicial. La IA analiza todo y genera un informe completo adaptado a tu contexto.",
    "whatYouGet": "¿Qué vas a recibir?",
    "previewCaption": "Así se ve tu análisis completo",
    "whyDayZero": "¿Por qué DayZero?",
    "hackathonBadge": "Creado en el hackathon Kaszek × Anthropic 2026 — Buenos Aires",
    "footerTagline": "DayZero — Tu copiloto de lanzamiento",
    "footerSubtext": "Hackathon Kaszek × Anthropic 2026",
    "sessionError": "Error al iniciar tu sesión",
    "backendError": "No se pudo iniciar el flujo en backend.",
    "featurePills": {
      "marketViability": "Viabilidad de mercado",
      "localCompetition": "Competencia local",
      "legalFramework": "Marco legal argentino",
      "launchPlan": "Plan de lanzamiento"
    },
    "trustCards": {
      "argentinaTitle": "Pensado para Argentina",
      "argentinaDesc": "Análisis de mercado local, marco legal argentino (monotributo, SAS), competencia real en tu ciudad.",
      "aiTitle": "IA que entiende tu contexto",
      "aiDesc": "No es un análisis genérico. Claude analiza tu idea, tus respuestas y tu ciudad para darte un plan personalizado.",
      "actionTitle": "De idea a acción en minutos",
      "actionDesc": "Viabilidad, competencia, kit de arranque, roadmap y landing page. Todo en un solo lugar."
    },
    "preview": {
      "viability": "Viabilidad",
      "competition": "Competencia",
      "rivals": "4 rivales",
      "clients": "Clientes",
      "segments": "3 segmentos",
      "legal": "Legal",
      "kit": "Kit",
      "roadmap": "Roadmap"
    }
  },
  "heroInput": {
    "stepIndicator": "Paso 1 de 3 — Contanos tu idea",
    "brandNameLabel": "¿Tenés un nombre para tu marca?",
    "optional": "(opcional)",
    "brandNamePlaceholder": "Ej: Empanadas La Abuela",
    "ideaLabel": "Tu idea de negocio",
    "ideaPlaceholder": "Ej: servicio de catering corporativo en Buenos Aires",
    "cityLabel": "Ciudad",
    "cityPlaceholder": "Ej: Buenos Aires",
    "investmentLabel": "Inversión inicial",
    "investmentPlaceholder": "Ej: 500.000",
    "currencyLabel": "ARS",
    "submitButton": "Iniciar misión",
    "submitHint": "La IA analiza mercado, competencia y arma tu plan de lanzamiento personalizado",
    "businessTypes": {
      "fisica": "Local físico",
      "fisicaDesc": "Tienda, restaurante, taller...",
      "digital": "Digital / Online",
      "digitalDesc": "App, e-commerce, servicio web...",
      "ambos": "Físico y digital",
      "ambosDesc": "Presencia en ambos canales"
    }
  },
  "login": {
    "title": "Iniciar sesión",
    "subtitle": "Ingresá tus credenciales para continuar",
    "emailLabel": "Email",
    "passwordLabel": "Contraseña",
    "showPassword": "Mostrar contraseña",
    "hidePassword": "Ocultar contraseña",
    "submitButton": "Ingresar",
    "loginError": "Error al iniciar sesión.",
    "connectionError": "No se pudo conectar con el servidor."
  },
  "questions": {
    "calibrating": "Calibrando tu misión",
    "calibratingHint": "Esto solo tarda unos segundos",
    "loadError": "Algo salió mal al cargar las preguntas",
    "noSession": "No existe una sesión activa. Volvá a la misión inicial para reiniciar el flujo.",
    "saveError": "No se pudieron guardar tus respuestas en el backend.",
    "continueError": "No se pudo continuar",
    "pageIndicator": "Página {currentPage} de {totalPages}",
    "introTitle": "Antes de generar tu análisis, necesitamos entender mejor tu idea.",
    "introDescription": "Respondé estas preguntas para que la IA pueda darte un análisis más preciso y personalizado.",
    "expandHint": "Podés elegir \"Quiero ampliar este punto\" para dar más contexto en cualquier pregunta.",
    "detailsPlaceholder": "Contanos más",
    "detailsTextarea": "Escribí todo el contexto que quieras compartir...",
    "nextButton": "Siguiente",
    "continueToAnalysis": "Continuar al análisis",
    "loadingMessages": {
      "0": "Leyendo tu idea con atención...",
      "1": "Identificando los puntos clave de tu misión...",
      "2": "Calibrando las preguntas más importantes para vos..."
    }
  },
  "analysis": {
    "preparing": "Preparando el análisis",
    "generating": "Generando tu análisis",
    "noSession": "No hay una sesión activa para generar el análisis.",
    "sessionExpired": "La sesión de análisis no existe o expiró. Iniciá una nueva idea.",
    "backendError": "No se pudo generar el análisis desde backend.",
    "errorTitle": "Error al construir el análisis",
    "planningMessages": {
      "0": "Evaluando qué secciones aplican a tu idea...",
      "1": "Determinando el alcance del análisis..."
    },
    "loadingMessages": {
      "0": "Analizando viabilidad y potencial de mercado...",
      "1": "Evaluando competencia y oportunidades...",
      "2": "Diseñando tu estrategia de monetización...",
      "3": "Construyendo tu kit de inicio y roadmap..."
    }
  },
  "dashboard": {
    "backToPanel": "Volver al panel",
    "analyzingRealTime": "Analizando en tiempo real...",
    "potential": "Potencial",
    "marketSummary": "Mercado {trend}, competencia {competition}.",
    "timeToIncome": "Tiempo estimado al primer ingreso: {time}.",
    "city": "{city}",
    "investment": "{amount}",
    "score": "/10",
    "viabilityLabel": "Viabilidad",
    "trendLabels": {
      "growing": "en crecimiento",
      "stable": "estable",
      "declining": "en descenso"
    },
    "competitionLabels": {
      "low": "baja",
      "moderate": "moderada",
      "high": "alta"
    },
    "verdicts": {
      "high": "Tu idea tiene un excelente potencial de mercado. Las condiciones son favorables para lanzar.",
      "medium": "El mercado muestra oportunidades, pero necesitarás diferenciarte de la competencia existente.",
      "low": "El mercado es desafiante. Considerá un enfoque de nicho o una propuesta de valor muy fuerte."
    }
  },
  "viabilitySection": {
    "title": "Potencial de Mercado",
    "dynamicScore": "Puntaje Dinámico de Potencial",
    "competitionLevel": "Nivel de Competencia",
    "entryBarrier": "Barrera de Entrada",
    "timeToIncome": "Tiempo al Primer Ingreso",
    "keyFindings": "Hallazgos Clave",
    "signalSources": "Fuentes del Puntaje (Señales en Vivo Simuladas)",
    "signalSourcesDesc": "Serp/Trends, Mercado Libre API e INDEC",
    "scoreImpact": "+{impact} score",
    "successfulIdeas": "Ideas Similares Exitosas",
    "similarity": "Similitud: {score}/100",
    "growth": "Crecimiento:",
    "traction": "Tracción:",
    "low": "Baja",
    "medium": "Media",
    "high": "Alta",
    "trendGrowing": "Tendencia: Creciendo",
    "trendStable": "Tendencia: Estable",
    "trendDeclining": "Tendencia: Bajando",
    "sourceGrowing": "Fuente: Creciendo",
    "sourceStable": "Fuente: Estable",
    "sourceDeclining": "Fuente: Bajando"
  },
  "competitorsSection": {
    "title": "Competidores identificados",
    "count": "{count} competidores{city}",
    "noCompetitors": "No se encontraron competidores",
    "noCompetitorsDesc": "La búsqueda web no devolvió resultados para este rubro{city}.",
    "verify": "Verificar",
    "searchQuery": "búsqueda: \"{query}\"",
    "strengths": "Fortalezas",
    "weaknesses": "Debilidades",
    "marketShareTitle": "Participación de mercado estimada",
    "marketShareDesc": "Distribución relativa entre competidores",
    "marketShareLabel": "Cuota de mercado"
  },
  "clientsSection": {
    "b2bTitle": "Clientes B2B Identificados{city}",
    "b2cTitle": "Segmentos de Clientes{city}",
    "approach": "Cómo abordarlo:",
    "contactRole": "Mejor rol de contacto:",
    "generateEmail": "Generar email específico",
    "generateMessage": "Generar mensaje"
  },
  "monetizationSection": {
    "title": "¿Cómo vas a ganar dinero?",
    "subtitle": "Modelo de negocio y precios sugeridos para tu idea",
    "modelType": "Tu tipo de modelo",
    "revenueStreams": "Tildá las fuentes de ingreso que aplican a tu negocio:",
    "revenuePercent": "% del ingreso",
    "pricingTitle": "¿Cuánto le cobrarías a tus clientes?",
    "pricingHint": "Arrastrá los sliders o tocá para ajustar los precios sugeridos por la IA. Los precios son mensuales y en ARS.",
    "pricePerMonth": "/mes · precio para tu cliente",
    "benchmarkTitle": "¿Cuánto cobra la competencia?",
    "priceLow": "Precio bajo",
    "priceAverage": "Promedio del mercado",
    "priceHigh": "Precio alto"
  },
  "legalSection": {
    "title": "Estructura Legal Recomendada",
    "evolution": "Evolución Recomendada",
    "month": "M{month}",
    "taxSimulator": "Simulador de Cargas Impositivas",
    "taxSimulatorDesc": "Estimá impuestos por categoría y facturación mensual",
    "taxCategory": "Categoría impositiva",
    "monthlyRevenue": "Facturación mensual (ARS)",
    "fixedQuota": "Cuota fija mensual",
    "vatEstimate": "Estimación IVA",
    "incomeEstimate": "Estimación Ganancias",
    "socialCharges": "Cargas sociales",
    "totalMonthly": "Total impuestos mensuales",
    "totalAnnual": "Impuestos anuales proyectados",
    "taxObligations": "Obligaciones Impositivas",
    "monthlyEstimate": "Estimación Mensual",
    "annualEstimate": "Estimación Anual",
    "benefits": "Beneficios de este régimen",
    "startProcedures": "Iniciar trámites legales e impositivos",
    "disclaimer": "Nota Importante",
    "disclaimerText": "Las estimaciones impositivas son simulaciones simplificadas basadas en datos públicos de AFIP/ARCA vigentes. No reemplazan el asesoramiento de un contador público. Consultá a un profesional antes de tomar decisiones fiscales."
  },
  "kitSection": {
    "title": "Kit de Inicio para tu Inversión",
    "subtitle": "Cómo distribuir ARS {investment}",
    "productComparison": "Comparación de productos entre plataformas",
    "ratingDelivery": "{rating}★ • {delivery}",
    "view": "Ver",
    "operatingReserve": "Reserva operativa recomendada"
  },
  "roadmapSection": {
    "validationTitle": "Plan de Validación de Idea",
    "validationSubtitle": "Validá antes de invertir fuerte",
    "successMetric": "Métrica de éxito: {metric}",
    "launchTools": "Herramientas de Lanzamiento",
    "launchToolsDesc": "Creación de página integrada y verificación de dominios disponibles",
    "createPage": "Crear mi página",
    "generating": "Generando...",
    "checkDomains": "Verificar dominios disponibles",
    "draftGenerated": "Borrador de página generado",
    "draftDescription": "Una estructura de una página lista para lanzar ya está incluida en tu flujo de trabajo.",
    "domainAvailability": "Disponibilidad de dominio",
    "domainAvailable": "disponible",
    "domainTaken": "ocupado",
    "noDomains": "No hay sugerencias de dominio disponibles para esta idea todavía.",
    "roadmapTitle": "Hoja de Ruta de Lanzamiento"
  },
  "obstaclesSection": {
    "title": "Obstáculos y Soluciones",
    "subtitle": "Casos de fracaso reales incluidos para evitar errores comunes",
    "howToFace": "Cómo enfrentarlo: ",
    "failureCase": "Caso de fracaso: {startup}",
    "whyFailed": "Por qué falló:",
    "lesson": "Lección:"
  },
  "financialSection": {
    "title": "Proyección financiera",
    "subtitle": "Registrá tus ingresos y gastos para ir llevando el balance de tu negocio",
    "initialInvestment": "Inversión inicial",
    "availableCapital": "Capital disponible",
    "committed": "{amount} ya comprometidos",
    "estimatedBalance": "Balance mensual estimado",
    "estimatedBalanceDesc": "Basado en todos los ítems, incluyendo los que todavía no confirmaste",
    "realBalance": "Balance mensual real",
    "realBalanceDesc": "Solo lo que ya confirmaste o pagaste",
    "confirmToSee": "Confirmá ítems para ver el balance real",
    "recoveryMessage": "Con este balance, recuperás tu inversión de {investment} en aproximadamente {months} meses",
    "monthlyIncome": "Ingresos mensuales",
    "incomeDesc": "¿De dónde entra la plata? Agregá cada fuente de ingreso...",
    "totalPerMonth": "total / mes",
    "addIncome": "Agregar fuente de ingreso",
    "monthlyExpenses": "Gastos mensuales",
    "expensesDesc": "Claude pre-cargó sugerencias para tu negocio. Ajustá montos, confirmá o agregá los tuyos.",
    "addExpense": "Agregar gasto",
    "statusGuide": "¿Cómo usar los estados?",
    "statusEstimated": "Estimado",
    "statusEstimatedDesc": "Todavía no lo confirmaste",
    "statusConfirmed": "Confirmado",
    "statusConfirmedDesc": "Ya sabés que va a ocurrir",
    "statusPaid": "Pagado",
    "statusPaidDesc": "Ya lo pagaste / cobraste",
    "changeStatus": "Cambiá el estado tocando acá",
    "expenseCategories": {
      "personal": "Personal",
      "local": "Local",
      "insumos": "Insumos",
      "marketing": "Marketing",
      "servicios": "Servicios",
      "otros": "Otros"
    },
    "incomeCategories": {
      "ventas": "Ventas",
      "servicios": "Servicios",
      "delivery": "Delivery",
      "eventos": "Eventos",
      "otros": "Otros"
    },
    "stateLabels": {
      "estimated": "Estimado",
      "confirmed": "Confirmado",
      "paid": "Pagado",
      "cancelled": "Cancelado"
    },
    "defaultExpenses": {
      "employee": "Empleado/a (1)",
      "rent": "Alquiler del local",
      "supplies": "Insumos y materias primas",
      "utilities": "Luz, gas y agua",
      "socialAds": "Redes sociales / publicidad",
      "hosting": "Hosting y dominio",
      "digitalTools": "Herramientas digitales",
      "digitalAds": "Publicidad digital",
      "freelancer": "Freelancer / colaborador",
      "accountant": "Contador / asesor"
    },
    "defaultIncomes": {
      "storeSales": "Ventas en mostrador / local",
      "delivery": "Pedidos por delivery",
      "catering": "Catering / eventos especiales",
      "subscriptions": "Suscripciones / licencias",
      "consulting": "Consultoría / servicios",
      "advertising": "Publicidad / afiliados"
    }
  },
  "landingCta": {
    "title": "Tu análisis está completo. Ahora aterrizá tu idea.",
    "description": "La IA ya tiene todo el contexto de tu análisis y puede generar una página lista para publicar.",
    "createPage": "Crear mi página",
    "comingSoon": "Próximamente: generá tu página automáticamente",
    "howToPublish": "¿Cómo publicarla gratis?",
    "features": {
      "heroSection": "Sección principal con propuesta de valor",
      "servicesSection": "Sección de servicios",
      "whatsappForm": "Formulario WhatsApp",
      "basicSeo": "SEO básico",
      "mobilePriority": "Prioridad móvil",
      "readyToPublish": "Lista para publicar"
    }
  }
}
```

- [ ] **Step 2: Create `messages/en.json`**

```json
{
  "common": {
    "newIdea": "New idea",
    "login": "Log in",
    "logout": "Log out",
    "toggleTheme": "Toggle theme",
    "backToStart": "Back to start",
    "brandName": "DayZero",
    "tagline": "Your launch copilot"
  },
  "metadata": {
    "title": "DayZero — Your Launch Copilot",
    "description": "Validate your business idea with AI. Viability analysis, competition, starter kit, and a personalized roadmap for Argentina."
  },
  "home": {
    "heroTitle1": "Launch the business",
    "heroTitle2": "you've always dreamed of",
    "heroDescription": "Describe your idea, your city, and your initial investment. AI analyzes everything and generates a full report tailored to your context.",
    "whatYouGet": "What will you get?",
    "previewCaption": "This is what your full analysis looks like",
    "whyDayZero": "Why DayZero?",
    "hackathonBadge": "Created at the Kaszek × Anthropic 2026 Hackathon — Buenos Aires",
    "footerTagline": "DayZero — Your launch copilot",
    "footerSubtext": "Kaszek × Anthropic 2026 Hackathon",
    "sessionError": "Error starting your session",
    "backendError": "Could not start the backend flow.",
    "featurePills": {
      "marketViability": "Market viability",
      "localCompetition": "Local competition",
      "legalFramework": "Argentine legal framework",
      "launchPlan": "Launch plan"
    },
    "trustCards": {
      "argentinaTitle": "Built for Argentina",
      "argentinaDesc": "Local market analysis, Argentine legal framework (monotributo, SAS), real competition in your city.",
      "aiTitle": "AI that understands your context",
      "aiDesc": "Not a generic analysis. Claude analyzes your idea, your answers, and your city to give you a personalized plan.",
      "actionTitle": "From idea to action in minutes",
      "actionDesc": "Viability, competition, starter kit, roadmap, and landing page. All in one place."
    },
    "preview": {
      "viability": "Viability",
      "competition": "Competition",
      "rivals": "4 rivals",
      "clients": "Clients",
      "segments": "3 segments",
      "legal": "Legal",
      "kit": "Kit",
      "roadmap": "Roadmap"
    }
  },
  "heroInput": {
    "stepIndicator": "Step 1 of 3 — Tell us your idea",
    "brandNameLabel": "Do you have a brand name?",
    "optional": "(optional)",
    "brandNamePlaceholder": "E.g.: Empanadas La Abuela",
    "ideaLabel": "Your business idea",
    "ideaPlaceholder": "E.g.: corporate catering service in Buenos Aires",
    "cityLabel": "City",
    "cityPlaceholder": "E.g.: Buenos Aires",
    "investmentLabel": "Initial investment",
    "investmentPlaceholder": "E.g.: 500,000",
    "currencyLabel": "ARS",
    "submitButton": "Start mission",
    "submitHint": "AI analyzes the market, competition, and builds your personalized launch plan",
    "businessTypes": {
      "fisica": "Physical location",
      "fisicaDesc": "Store, restaurant, workshop...",
      "digital": "Digital / Online",
      "digitalDesc": "App, e-commerce, web service...",
      "ambos": "Physical & digital",
      "ambosDesc": "Presence in both channels"
    }
  },
  "login": {
    "title": "Log in",
    "subtitle": "Enter your credentials to continue",
    "emailLabel": "Email",
    "passwordLabel": "Password",
    "showPassword": "Show password",
    "hidePassword": "Hide password",
    "submitButton": "Log in",
    "loginError": "Login failed.",
    "connectionError": "Could not connect to the server."
  },
  "questions": {
    "calibrating": "Calibrating your mission",
    "calibratingHint": "This only takes a few seconds",
    "loadError": "Something went wrong loading the questions",
    "noSession": "No active session found. Go back to the initial mission to restart the flow.",
    "saveError": "Could not save your answers to the backend.",
    "continueError": "Could not continue",
    "pageIndicator": "Page {currentPage} of {totalPages}",
    "introTitle": "Before generating your analysis, we need to better understand your idea.",
    "introDescription": "Answer these questions so AI can give you a more precise and personalized analysis.",
    "expandHint": "You can choose \"I want to expand on this point\" to give more context on any question.",
    "detailsPlaceholder": "Tell us more",
    "detailsTextarea": "Write all the context you want to share...",
    "nextButton": "Next",
    "continueToAnalysis": "Continue to analysis",
    "loadingMessages": {
      "0": "Reading your idea carefully...",
      "1": "Identifying the key points of your mission...",
      "2": "Calibrating the most important questions for you..."
    }
  },
  "analysis": {
    "preparing": "Preparing the analysis",
    "generating": "Generating your analysis",
    "noSession": "No active session to generate the analysis.",
    "sessionExpired": "The analysis session doesn't exist or has expired. Start a new idea.",
    "backendError": "Could not generate the analysis from the backend.",
    "errorTitle": "Error building the analysis",
    "planningMessages": {
      "0": "Evaluating which sections apply to your idea...",
      "1": "Determining the scope of the analysis..."
    },
    "loadingMessages": {
      "0": "Analyzing viability and market potential...",
      "1": "Evaluating competition and opportunities...",
      "2": "Designing your monetization strategy...",
      "3": "Building your starter kit and roadmap..."
    }
  },
  "dashboard": {
    "backToPanel": "Back to panel",
    "analyzingRealTime": "Analyzing in real time...",
    "potential": "Potential",
    "marketSummary": "{trend} market, {competition} competition.",
    "timeToIncome": "Estimated time to first income: {time}.",
    "city": "{city}",
    "investment": "{amount}",
    "score": "/10",
    "viabilityLabel": "Viability",
    "trendLabels": {
      "growing": "growing",
      "stable": "stable",
      "declining": "declining"
    },
    "competitionLabels": {
      "low": "low",
      "moderate": "moderate",
      "high": "high"
    },
    "verdicts": {
      "high": "Your idea has excellent market potential. Conditions are favorable for launch.",
      "medium": "The market shows opportunities, but you'll need to differentiate from existing competition.",
      "low": "The market is challenging. Consider a niche approach or a very strong value proposition."
    }
  },
  "viabilitySection": {
    "title": "Market Potential",
    "dynamicScore": "Dynamic Potential Score",
    "competitionLevel": "Competition Level",
    "entryBarrier": "Entry Barrier",
    "timeToIncome": "Time to First Income",
    "keyFindings": "Key Findings",
    "signalSources": "Score Sources (Simulated Live Signals)",
    "signalSourcesDesc": "Serp/Trends, Mercado Libre API & INDEC",
    "scoreImpact": "+{impact} score",
    "successfulIdeas": "Similar Successful Ideas",
    "similarity": "Similarity: {score}/100",
    "growth": "Growth:",
    "traction": "Traction:",
    "low": "Low",
    "medium": "Medium",
    "high": "High",
    "trendGrowing": "Trend: Growing",
    "trendStable": "Trend: Stable",
    "trendDeclining": "Trend: Declining",
    "sourceGrowing": "Source: Growing",
    "sourceStable": "Source: Stable",
    "sourceDeclining": "Source: Declining"
  },
  "competitorsSection": {
    "title": "Identified competitors",
    "count": "{count} competitors{city}",
    "noCompetitors": "No competitors found",
    "noCompetitorsDesc": "The web search returned no results for this sector{city}.",
    "verify": "Verify",
    "searchQuery": "search: \"{query}\"",
    "strengths": "Strengths",
    "weaknesses": "Weaknesses",
    "marketShareTitle": "Estimated market share",
    "marketShareDesc": "Relative distribution among competitors",
    "marketShareLabel": "Market share"
  },
  "clientsSection": {
    "b2bTitle": "Identified B2B Clients{city}",
    "b2cTitle": "Client Segments{city}",
    "approach": "How to approach:",
    "contactRole": "Best contact role:",
    "generateEmail": "Generate specific email",
    "generateMessage": "Generate message"
  },
  "monetizationSection": {
    "title": "How will you make money?",
    "subtitle": "Business model and suggested pricing for your idea",
    "modelType": "Your model type",
    "revenueStreams": "Check the revenue streams that apply to your business:",
    "revenuePercent": "% of revenue",
    "pricingTitle": "How much would you charge your clients?",
    "pricingHint": "Drag the sliders or tap to adjust the AI-suggested prices. Prices are monthly and in ARS.",
    "pricePerMonth": "/month · price for your client",
    "benchmarkTitle": "How much does the competition charge?",
    "priceLow": "Low price",
    "priceAverage": "Market average",
    "priceHigh": "High price"
  },
  "legalSection": {
    "title": "Recommended Legal Structure",
    "evolution": "Recommended Evolution",
    "month": "M{month}",
    "taxSimulator": "Tax Burden Simulator",
    "taxSimulatorDesc": "Estimate taxes by category and monthly revenue",
    "taxCategory": "Tax category",
    "monthlyRevenue": "Monthly revenue (ARS)",
    "fixedQuota": "Fixed monthly quota",
    "vatEstimate": "VAT estimate",
    "incomeEstimate": "Income tax estimate",
    "socialCharges": "Social charges",
    "totalMonthly": "Total monthly taxes",
    "totalAnnual": "Projected annual taxes",
    "taxObligations": "Tax Obligations",
    "monthlyEstimate": "Monthly Estimate",
    "annualEstimate": "Annual Estimate",
    "benefits": "Benefits of this regime",
    "startProcedures": "Start legal and tax procedures",
    "disclaimer": "Important Note",
    "disclaimerText": "Tax estimates are simplified simulations based on public AFIP/ARCA data. They do not replace advice from a certified accountant. Consult a professional before making tax decisions."
  },
  "kitSection": {
    "title": "Starter Kit for Your Investment",
    "subtitle": "How to distribute ARS {investment}",
    "productComparison": "Product comparison across platforms",
    "ratingDelivery": "{rating}★ • {delivery}",
    "view": "View",
    "operatingReserve": "Recommended operating reserve"
  },
  "roadmapSection": {
    "validationTitle": "Idea Validation Plan",
    "validationSubtitle": "Validate before investing heavily",
    "successMetric": "Success metric: {metric}",
    "launchTools": "Launch Tools",
    "launchToolsDesc": "Integrated page creation and available domain verification",
    "createPage": "Create my page",
    "generating": "Generating...",
    "checkDomains": "Check available domains",
    "draftGenerated": "Page draft generated",
    "draftDescription": "A one-page structure ready to launch is already included in your workflow.",
    "domainAvailability": "Domain availability",
    "domainAvailable": "available",
    "domainTaken": "taken",
    "noDomains": "No domain suggestions available for this idea yet.",
    "roadmapTitle": "Launch Roadmap"
  },
  "obstaclesSection": {
    "title": "Obstacles & Solutions",
    "subtitle": "Real failure cases included to help you avoid common mistakes",
    "howToFace": "How to face it: ",
    "failureCase": "Failure case: {startup}",
    "whyFailed": "Why it failed:",
    "lesson": "Lesson:"
  },
  "financialSection": {
    "title": "Financial projection",
    "subtitle": "Track your income and expenses to keep your business balance up to date",
    "initialInvestment": "Initial investment",
    "availableCapital": "Available capital",
    "committed": "{amount} already committed",
    "estimatedBalance": "Estimated monthly balance",
    "estimatedBalanceDesc": "Based on all items, including those you haven't confirmed yet",
    "realBalance": "Actual monthly balance",
    "realBalanceDesc": "Only what you've already confirmed or paid",
    "confirmToSee": "Confirm items to see the actual balance",
    "recoveryMessage": "At this rate, you'll recover your investment of {investment} in approximately {months} months",
    "monthlyIncome": "Monthly income",
    "incomeDesc": "Where does the money come in? Add each income source...",
    "totalPerMonth": "total / month",
    "addIncome": "Add income source",
    "monthlyExpenses": "Monthly expenses",
    "expensesDesc": "Claude pre-loaded suggestions for your business. Adjust amounts, confirm, or add your own.",
    "addExpense": "Add expense",
    "statusGuide": "How to use statuses?",
    "statusEstimated": "Estimated",
    "statusEstimatedDesc": "Not confirmed yet",
    "statusConfirmed": "Confirmed",
    "statusConfirmedDesc": "You know it will happen",
    "statusPaid": "Paid",
    "statusPaidDesc": "Already paid / collected",
    "changeStatus": "Tap here to change the status",
    "expenseCategories": {
      "personal": "Staff",
      "local": "Location",
      "insumos": "Supplies",
      "marketing": "Marketing",
      "servicios": "Services",
      "otros": "Other"
    },
    "incomeCategories": {
      "ventas": "Sales",
      "servicios": "Services",
      "delivery": "Delivery",
      "eventos": "Events",
      "otros": "Other"
    },
    "stateLabels": {
      "estimated": "Estimated",
      "confirmed": "Confirmed",
      "paid": "Paid",
      "cancelled": "Cancelled"
    },
    "defaultExpenses": {
      "employee": "Employee (1)",
      "rent": "Location rent",
      "supplies": "Supplies & raw materials",
      "utilities": "Electricity, gas & water",
      "socialAds": "Social media / advertising",
      "hosting": "Hosting & domain",
      "digitalTools": "Digital tools",
      "digitalAds": "Digital advertising",
      "freelancer": "Freelancer / collaborator",
      "accountant": "Accountant / advisor"
    },
    "defaultIncomes": {
      "storeSales": "In-store / counter sales",
      "delivery": "Delivery orders",
      "catering": "Catering / special events",
      "subscriptions": "Subscriptions / licenses",
      "consulting": "Consulting / services",
      "advertising": "Advertising / affiliates"
    }
  },
  "landingCta": {
    "title": "Your analysis is complete. Now bring your idea to life.",
    "description": "AI already has all the context from your analysis and can generate a page ready to publish.",
    "createPage": "Create my page",
    "comingSoon": "Coming soon: generate your page automatically",
    "howToPublish": "How to publish for free?",
    "features": {
      "heroSection": "Hero section with value proposition",
      "servicesSection": "Services section",
      "whatsappForm": "WhatsApp form",
      "basicSeo": "Basic SEO",
      "mobilePriority": "Mobile priority",
      "readyToPublish": "Ready to publish"
    }
  }
}
```

- [ ] **Step 3: Commit**

```bash
git add messages/
git commit -m "feat(i18n): add Spanish and English translation files"
```

---

## Task 3: Create middleware and restructure layouts

**Files:**
- Create: `middleware.ts`
- Create: `app/[locale]/layout.tsx`
- Modify: `app/layout.tsx`

- [ ] **Step 1: Create `middleware.ts`**

```ts
import createMiddleware from "next-intl/middleware";
import { routing } from "./i18n/routing";

export default createMiddleware(routing);

export const config = {
  matcher: [
    // Match all pathnames except api, _next, and static files
    "/((?!api|_next|.*\\..*).*)",
  ],
};
```

- [ ] **Step 2: Create `app/[locale]/layout.tsx`**

```tsx
import type { Metadata } from "next";
import { notFound } from "next/navigation";
import { hasLocale } from "next-intl";
import { setRequestLocale, getTranslations } from "next-intl/server";
import { routing } from "@/i18n/routing";

type Props = {
  children: React.ReactNode;
  params: Promise<{ locale: string }>;
};

export async function generateStaticParams() {
  return routing.locales.map((locale) => ({ locale }));
}

export async function generateMetadata({
  params,
}: {
  params: Promise<{ locale: string }>;
}): Promise<Metadata> {
  const { locale } = await params;
  const t = await getTranslations({ locale, namespace: "metadata" });

  return {
    title: t("title"),
    description: t("description"),
  };
}

export default async function LocaleLayout({ children, params }: Props) {
  const { locale } = await params;

  if (!hasLocale(routing.locales, locale)) {
    notFound();
  }

  setRequestLocale(locale);

  const htmlLang = locale === "es" ? "es-AR" : "en";

  return (
    <html lang={htmlLang} className="bg-background" suppressHydrationWarning>
      <body suppressHydrationWarning>
        {children}
      </body>
    </html>
  );
}
```

Note: This layout intentionally omits fonts and ThemeProvider — those stay in the root layout.

Wait — actually, `next-intl` with App Router requires the `<html>` tag to be in the `[locale]/layout.tsx` so the `lang` attribute is dynamic. But fonts and providers need to be available to all pages. Let me reconsider.

The correct approach: the root `app/layout.tsx` should NOT have an `<html>` tag (just a fragment). The `app/[locale]/layout.tsx` should have `<html>` with the dynamic `lang`, fonts, and ThemeProvider.

- [ ] **Step 3: Modify `app/layout.tsx` to be a minimal root layout**

Replace the entire file with:

```tsx
import type { ReactNode } from "react";

type Props = {
  children: ReactNode;
};

// Root layout is intentionally minimal — all UI is in [locale]/layout.tsx.
// Next.js requires a root layout, but next-intl moves <html> to the locale layout.
export default function RootLayout({ children }: Props) {
  return children;
}
```

- [ ] **Step 4: Update `app/[locale]/layout.tsx` with full content**

Replace the file created in Step 2 with the full version that includes fonts, ThemeProvider, and Analytics:

```tsx
import type { Metadata, Viewport } from "next";
import { notFound } from "next/navigation";
import { Sora, Plus_Jakarta_Sans } from "next/font/google";
import { Analytics } from "@vercel/analytics/next";
import { hasLocale } from "next-intl";
import { NextIntlClientProvider } from "next-intl";
import { setRequestLocale, getTranslations, getMessages } from "next-intl/server";
import { ThemeProvider } from "@/components/theme-provider";
import { routing } from "@/i18n/routing";
import "../globals.css";
import "leaflet/dist/leaflet.css";

const sora = Sora({
  subsets: ["latin"],
  variable: "--font-heading",
  display: "swap",
  weight: ["600", "700", "800"],
});

const plusJakarta = Plus_Jakarta_Sans({
  subsets: ["latin"],
  variable: "--font-body",
  display: "swap",
});

export const viewport: Viewport = {
  themeColor: [
    { media: "(prefers-color-scheme: light)", color: "#fafafa" },
    { media: "(prefers-color-scheme: dark)", color: "#0a0a0a" },
  ],
};

export async function generateStaticParams() {
  return routing.locales.map((locale) => ({ locale }));
}

export async function generateMetadata({
  params,
}: {
  params: Promise<{ locale: string }>;
}): Promise<Metadata> {
  const { locale } = await params;
  const t = await getTranslations({ locale, namespace: "metadata" });

  return {
    title: t("title"),
    description: t("description"),
    generator: "v0.app",
    icons: {
      icon: [
        { url: "/icon-light-32x32.png", media: "(prefers-color-scheme: light)" },
        { url: "/icon-dark-32x32.png", media: "(prefers-color-scheme: dark)" },
        { url: "/icon.svg", type: "image/svg+xml" },
      ],
      apple: "/apple-icon.png",
    },
  };
}

type Props = {
  children: React.ReactNode;
  params: Promise<{ locale: string }>;
};

export default async function LocaleLayout({ children, params }: Props) {
  const { locale } = await params;

  if (!hasLocale(routing.locales, locale)) {
    notFound();
  }

  setRequestLocale(locale);

  const messages = await getMessages();
  const htmlLang = locale === "es" ? "es-AR" : "en";

  return (
    <html lang={htmlLang} className="bg-background" suppressHydrationWarning>
      <body
        className={`${sora.variable} ${plusJakarta.variable} font-sans antialiased`}
        suppressHydrationWarning
      >
        <NextIntlClientProvider messages={messages}>
          <ThemeProvider
            attribute="class"
            defaultTheme="dark"
            enableSystem
            disableTransitionOnChange
          >
            {children}
          </ThemeProvider>
        </NextIntlClientProvider>
        {process.env.NODE_ENV === "production" && <Analytics />}
      </body>
    </html>
  );
}
```

- [ ] **Step 5: Verify the build compiles**

```bash
pnpm build
```

Expected: build succeeds (pages won't render yet since they haven't moved).

- [ ] **Step 6: Commit**

```bash
git add middleware.ts app/layout.tsx app/\[locale\]/layout.tsx
git commit -m "feat(i18n): add middleware and locale-aware layout"
```

---

## Task 4: Move pages into `[locale]` segment

**Files:**
- Move: `app/page.tsx` → `app/[locale]/page.tsx`
- Move: `app/login/page.tsx` → `app/[locale]/login/page.tsx`
- Move: `app/preguntas-claude/page.tsx` → `app/[locale]/preguntas-claude/page.tsx`
- Move: `app/analisis/page.tsx` → `app/[locale]/analisis/page.tsx`

- [ ] **Step 1: Create directories and move files**

```bash
mkdir -p app/\[locale\]/login app/\[locale\]/preguntas-claude app/\[locale\]/analisis
git mv app/page.tsx app/\[locale\]/page.tsx
git mv app/login/page.tsx app/\[locale\]/login/page.tsx
git mv app/preguntas-claude/page.tsx app/\[locale\]/preguntas-claude/page.tsx
git mv app/analisis/page.tsx app/\[locale\]/analisis/page.tsx
```

Clean up empty directories:

```bash
rmdir app/login app/preguntas-claude app/analisis 2>/dev/null || true
```

- [ ] **Step 2: Verify dev server starts**

```bash
pnpm dev
```

Visit `http://localhost:3000` — should load the home page (Spanish, no prefix). Visit `http://localhost:3000/en` — should load the home page (same content for now, translations not yet wired).

- [ ] **Step 3: Commit**

```bash
git add -A
git commit -m "refactor(i18n): move pages into [locale] route segment"
```

---

## Task 5: Wire translations into the header and add language toggle

**Files:**
- Create: `components/language-toggle.tsx`
- Modify: `components/header.tsx`

- [ ] **Step 1: Create `components/language-toggle.tsx`**

```tsx
"use client";

import { useLocale } from "next-intl";
import { usePathname, useRouter } from "@/i18n/navigation";
import { Button } from "@/components/ui/button";
import { Globe } from "lucide-react";
import type { Locale } from "@/i18n/config";

export function LanguageToggle() {
  const locale = useLocale() as Locale;
  const router = useRouter();
  const pathname = usePathname();

  const nextLocale: Locale = locale === "es" ? "en" : "es";
  const label = nextLocale.toUpperCase();

  const handleSwitch = () => {
    router.replace(pathname, { locale: nextLocale });
  };

  return (
    <Button
      variant="ghost"
      size="sm"
      onClick={handleSwitch}
      className="gap-1.5 px-2"
    >
      <Globe className="h-4 w-4" />
      <span className="text-xs font-medium">{label}</span>
    </Button>
  );
}
```

- [ ] **Step 2: Modify `components/header.tsx`**

Replace the entire file:

```tsx
"use client";

import { useEffect, useState } from "react";
import { LogIn, LogOut, Moon, Rocket, Sun } from "lucide-react";
import { useTheme } from "next-themes";
import { useTranslations, useLocale } from "next-intl";
import { usePathname, useRouter, Link } from "@/i18n/navigation";
import { Button } from "@/components/ui/button";
import { SpaceProgress } from "@/components/space-progress";
import { LanguageToggle } from "@/components/language-toggle";
import { clearFlowStorage, getBrandIdentity, getBusinessInput } from "@/lib/flow-storage";
import type { SpaceProgressProps } from "@/components/space-progress";

const STEP_MAP: Record<string, SpaceProgressProps["currentStep"]> = {
  "/": 1,
  "/preguntas-claude": 2,
  "/analisis": 3,
};

interface AuthUser {
  id: string;
  email: string;
  name: string;
}

export function Header() {
  const t = useTranslations("common");
  const { theme, setTheme } = useTheme();
  const router = useRouter();
  const pathname = usePathname();
  const showReset = pathname !== "/" && pathname !== "/login";

  const [user, setUser] = useState<AuthUser | null>(null);
  const [brandName, setBrandName] = useState<string | null>(null);
  const showBrandName = showReset && brandName;

  const currentStep = STEP_MAP[pathname] ?? null;
  const showProgress = currentStep !== null && pathname !== "/login";

  useEffect(() => {
    const input = getBusinessInput();
    const stored = getBrandIdentity();
    setBrandName(stored?.name ?? input?.brandName ?? null);
  }, [pathname]);

  useEffect(() => {
    fetch("/api/auth/me")
      .then((res) => res.json())
      .then((json) => {
        if (json.ok) setUser(json.data.user);
      })
      .catch(() => {});
  }, [pathname]);

  const handleReset = () => {
    clearFlowStorage();
    router.push("/");
  };

  const handleLogout = async () => {
    await fetch("/api/auth/logout", { method: "POST" });
    setUser(null);
    clearFlowStorage();
    router.push("/login");
  };

  return (
    <header className="sticky top-0 z-50 w-full border-b border-border/40 bg-background/80 backdrop-blur-xl">
      <div className="mx-auto flex h-14 max-w-7xl items-center justify-between px-4 sm:px-6 lg:px-8">
        <button
          onClick={handleReset}
          className="flex items-center gap-2 transition-opacity hover:opacity-80"
        >
          <div className="flex h-8 w-8 items-center justify-center rounded-lg bg-primary">
            <Rocket className="h-4 w-4 text-primary-foreground" />
          </div>
          <span className="text-base font-semibold tracking-tight">DayZero</span>
          {showBrandName && (
            <>
              <span className="text-muted-foreground/40 text-base font-light">/</span>
              <span className="text-sm font-medium text-primary">{brandName}</span>
            </>
          )}
        </button>

        {showProgress && (
          <SpaceProgress
            currentStep={currentStep as SpaceProgressProps["currentStep"]}
            brandName={brandName ?? undefined}
          />
        )}

        <div className="flex items-center gap-2">
          {showReset && (
            <Button variant="outline" size="sm" onClick={handleReset}>
              {t("newIdea")}
            </Button>
          )}

          {user ? (
            <>
              <span className="hidden text-sm text-muted-foreground sm:block">
                {user.name}
              </span>
              <Button variant="ghost" size="sm" onClick={handleLogout} className="gap-2">
                <LogOut className="h-4 w-4" />
                <span className="hidden sm:inline">{t("logout")}</span>
              </Button>
            </>
          ) : (
            pathname !== "/login" && (
              <Button
                variant="ghost"
                size="sm"
                onClick={() => router.push("/login")}
                className="gap-2"
              >
                <LogIn className="h-4 w-4" />
                <span className="hidden sm:inline">{t("login")}</span>
              </Button>
            )
          )}

          <LanguageToggle />

          <Button
            variant="ghost"
            size="icon"
            onClick={() => setTheme(theme === "dark" ? "light" : "dark")}
            className="h-8 w-8"
          >
            <Sun className="h-4 w-4 rotate-0 scale-100 transition-all dark:-rotate-90 dark:scale-0" />
            <Moon className="absolute h-4 w-4 rotate-90 scale-0 transition-all dark:rotate-0 dark:scale-100" />
            <span className="sr-only">{t("toggleTheme")}</span>
          </Button>
        </div>
      </div>
    </header>
  );
}
```

Key changes:
- Import `useTranslations`, `useLocale` from `next-intl`
- Import `usePathname`, `useRouter` from `@/i18n/navigation` (replaces `next/navigation`)
- Add `LanguageToggle` between auth buttons and theme toggle
- Replace hardcoded Spanish strings with `t()` calls

- [ ] **Step 3: Verify in browser**

```bash
pnpm dev
```

Visit `http://localhost:3000`. Header should show "Nueva idea", "Ingresar", "Cambiar tema" (Spanish). Click the EN button. URL changes to `/en`. Header now shows "New idea", "Log in", "Toggle theme".

- [ ] **Step 4: Commit**

```bash
git add components/language-toggle.tsx components/header.tsx
git commit -m "feat(i18n): wire translations into header and add language toggle"
```

---

## Task 6: Wire translations into home page and hero input

**Files:**
- Modify: `app/[locale]/page.tsx`
- Modify: `components/hero-input.tsx`

- [ ] **Step 1: Modify `components/hero-input.tsx`**

Replace the entire file:

```tsx
"use client";

import { useState } from "react";
import { Rocket, MapPin, DollarSign, Tag } from "lucide-react";
import { useTranslations, useLocale } from "next-intl";

export type BusinessType = "fisica" | "digital" | "ambos";

export interface BusinessInputData {
  idea: string;
  city?: string;
  investment?: number;
  brandName?: string;
  businessType?: BusinessType;
}

interface HeroInputProps {
  onSubmit: (data: BusinessInputData) => void;
}

function formatNumber(value: string, locale: string): string {
  const num = value.replace(/\D/g, "");
  if (!num) return "";
  return Number(num).toLocaleString(locale === "es" ? "es-AR" : "en-US");
}

function parseNumber(value: string): number {
  return Number(value.replace(/\D/g, "")) || 0;
}

export function HeroInput({ onSubmit }: HeroInputProps) {
  const t = useTranslations("heroInput");
  const locale = useLocale();
  const [idea, setIdea] = useState("");
  const [city, setCity] = useState("");
  const [investment, setInvestment] = useState("");
  const [brandName, setBrandName] = useState("");
  const [businessType, setBusinessType] = useState<BusinessType | null>(null);

  const BUSINESS_TYPES: { value: BusinessType; emoji: string; label: string; desc: string }[] = [
    { value: "fisica", emoji: "🏪", label: t("businessTypes.fisica"), desc: t("businessTypes.fisicaDesc") },
    { value: "digital", emoji: "💻", label: t("businessTypes.digital"), desc: t("businessTypes.digitalDesc") },
    { value: "ambos", emoji: "🔄", label: t("businessTypes.ambos"), desc: t("businessTypes.ambosDesc") },
  ];

  const handleSubmit = (e: React.FormEvent) => {
    e.preventDefault();
    if (!idea.trim()) return;

    onSubmit({
      idea,
      city: city.trim() || undefined,
      investment: parseNumber(investment) || undefined,
      brandName: brandName.trim() || undefined,
      businessType: businessType ?? undefined,
    });
  };

  const handleInvestmentChange = (e: React.ChangeEvent<HTMLInputElement>) => {
    const formatted = formatNumber(e.target.value, locale);
    setInvestment(formatted);
  };

  return (
    <div className="relative flex flex-col items-center px-4 py-10 sm:py-14">
      <div className="relative mx-auto w-full max-w-3xl">
        <span className="dz-twinkle pointer-events-none absolute -left-4 top-6 hidden h-1.5 w-1.5 rounded-full bg-primary/50 sm:block" />
        <span
          className="dz-twinkle pointer-events-none absolute -right-3 top-20 hidden h-1 w-1 rounded-full bg-primary/40 sm:block"
          style={{ animationDelay: "2s" }}
        />
        <span className="dz-float pointer-events-none absolute right-10 -top-2 hidden h-2 w-2 rounded-full bg-primary/20 sm:block" />

        <div className="dz-enter-2 mx-auto mt-3 flex items-center justify-center gap-2">
          <span className="flex h-5 w-5 items-center justify-center rounded-full bg-primary text-[10px] font-bold text-primary-foreground">
            1
          </span>
          <span className="text-sm text-muted-foreground">
            {t("stepIndicator")}
          </span>
        </div>

        <form onSubmit={handleSubmit} className="dz-enter-3 mt-8">
          <div className="dz-form-card rounded-2xl border border-border/50 bg-card/80 p-6 backdrop-blur-xl sm:p-8 dark:border-primary/[0.08] dark:bg-white/[0.03]">
            <div className="space-y-2">
              <label htmlFor="brandName" className="text-sm font-medium text-foreground">
                {t("brandNameLabel")}{" "}
                <span className="font-normal text-muted-foreground">{t("optional")}</span>
              </label>
              <div className="relative">
                <Tag className="absolute left-3 top-1/2 h-4 w-4 -translate-y-1/2 text-muted-foreground" />
                <input
                  id="brandName"
                  type="text"
                  value={brandName}
                  onChange={(e) => setBrandName(e.target.value)}
                  placeholder={t("brandNamePlaceholder")}
                  className="dz-input w-full rounded-xl border border-input bg-background/50 py-3 pl-10 pr-4 text-base text-foreground placeholder:text-muted-foreground dark:bg-white/[0.02]"
                />
              </div>
            </div>

            <div className="my-6 border-t border-border/50 dark:border-white/[0.06]" />

            <div className="space-y-2">
              <label htmlFor="idea" className="text-sm font-medium text-foreground">
                {t("ideaLabel")}
              </label>
              <textarea
                id="idea"
                value={idea}
                onChange={(e) => setIdea(e.target.value)}
                placeholder={t("ideaPlaceholder")}
                className="dz-input min-h-[110px] w-full resize-none rounded-xl border border-input bg-background/50 p-4 text-base text-foreground placeholder:text-muted-foreground dark:bg-white/[0.02]"
              />
            </div>

            <div className="mt-5 grid gap-4 sm:grid-cols-2">
              <div className="space-y-2">
                <label htmlFor="city" className="text-sm font-medium text-foreground">
                  {t("cityLabel")}
                </label>
                <div className="relative">
                  <MapPin className="absolute left-3 top-1/2 h-4 w-4 -translate-y-1/2 text-muted-foreground" />
                  <input
                    id="city"
                    type="text"
                    value={city}
                    onChange={(e) => setCity(e.target.value)}
                    placeholder={t("cityPlaceholder")}
                    className="dz-input w-full rounded-xl border border-input bg-background/50 py-3 pl-10 pr-4 text-base text-foreground placeholder:text-muted-foreground dark:bg-white/[0.02]"
                  />
                </div>
              </div>

              <div className="space-y-2">
                <label htmlFor="investment" className="text-sm font-medium text-foreground">
                  {t("investmentLabel")}
                </label>
                <div className="relative">
                  <DollarSign className="absolute left-3 top-1/2 h-4 w-4 -translate-y-1/2 text-muted-foreground" />
                  <input
                    id="investment"
                    type="text"
                    value={investment}
                    onChange={handleInvestmentChange}
                    placeholder={t("investmentPlaceholder")}
                    className="dz-input w-full rounded-xl border border-input bg-background/50 py-3 pl-10 pr-4 text-base text-foreground placeholder:text-muted-foreground dark:bg-white/[0.02]"
                  />
                  <span className="absolute right-3 top-1/2 -translate-y-1/2 text-sm text-muted-foreground">
                    {t("currencyLabel")}
                  </span>
                </div>
              </div>
            </div>
          </div>

          <div className="dz-enter-4 mt-8 flex flex-col items-center gap-4">
            <button
              type="submit"
              disabled={!idea.trim()}
              className="dz-cta group inline-flex h-14 items-center justify-center gap-3 rounded-xl px-10 text-lg font-semibold text-white"
            >
              {t("submitButton")}
              <Rocket className="h-5 w-5 transition-transform duration-300 group-hover:-translate-y-0.5 group-hover:-rotate-12" />
            </button>
            <p className="text-center text-sm text-muted-foreground">
              {t("submitHint")}
            </p>
          </div>
        </form>
      </div>
    </div>
  );
}
```

- [ ] **Step 2: Modify `app/[locale]/page.tsx`**

Replace the entire file. Key changes: import `useTranslations` from `next-intl`, import `useRouter` from `@/i18n/navigation`, replace all hardcoded strings with `t()` calls.

```tsx
"use client";

import { useState } from "react";
import {
  AlertTriangle,
  TrendingUp,
  Users,
  Scale,
  Map,
  MapPin,
  Sparkles,
  Zap,
} from "lucide-react";
import { useTranslations } from "next-intl";
import { useRouter } from "@/i18n/navigation";
import { HeroInput, BusinessInputData } from "@/components/hero-input";
import { Header } from "@/components/header";
import { Card, CardContent } from "@/components/ui/card";
import { createFlowSession, isApiClientError } from "@/lib/flow-api";
import { saveBusinessInput, saveFlowId } from "@/lib/flow-storage";

const FEATURE_ICONS = [TrendingUp, Users, Scale, Map];
const FEATURE_KEYS = ["marketViability", "localCompetition", "legalFramework", "launchPlan"] as const;

const TRUST_ICONS = [MapPin, Sparkles, Zap];
const TRUST_KEYS = ["argentina", "ai", "action"] as const;

export default function Home() {
  const t = useTranslations("home");
  const tInput = useTranslations("heroInput");
  const router = useRouter();
  const [submitError, setSubmitError] = useState<string | null>(null);
  const [requestId, setRequestId] = useState<string | null>(null);

  const handleSubmit = async (data: BusinessInputData) => {
    setSubmitError(null);
    setRequestId(null);

    try {
      const flowId = await createFlowSession(data);
      saveBusinessInput(data);
      saveFlowId(flowId);
      router.push("/preguntas-claude");
    } catch (error) {
      if (isApiClientError(error)) {
        setSubmitError(error.message);
        setRequestId(error.requestId ?? null);
      } else {
        setSubmitError(t("backendError"));
      }
    }
  };

  return (
    <main className="dz-hero-bg relative min-h-screen overflow-hidden">
      <div className="pointer-events-none absolute inset-0 opacity-0 dark:opacity-100">
        <div className="dz-stars absolute inset-0" />
        <div className="dz-noise absolute inset-0" />
      </div>
      <div className="pointer-events-none absolute inset-0 dark:opacity-0">
        <div className="dz-noise absolute inset-0" />
      </div>

      <div className="relative z-10">
        <Header />

        {submitError && (
          <div className="mx-auto w-full max-w-3xl px-4 pt-6">
            <Card className="border-destructive/30 bg-destructive/5">
              <CardContent className="space-y-2 p-4">
                <p className="flex items-center gap-2 text-sm font-medium text-destructive">
                  <AlertTriangle className="h-4 w-4" />
                  {t("sessionError")}
                </p>
                <p className="text-sm text-foreground/80">{submitError}</p>
                {requestId && (
                  <p className="text-xs text-muted-foreground">Request ID: {requestId}</p>
                )}
              </CardContent>
            </Card>
          </div>
        )}

        <section className="mx-auto max-w-4xl px-4 pt-16 text-center sm:pt-20">
          <h1 className="dz-enter-1 font-display text-balance text-4xl font-extrabold tracking-tight sm:text-5xl md:text-6xl lg:text-7xl">
            {t("heroTitle1")}{" "}
            <span className="bg-gradient-to-r from-primary via-[oklch(0.6_0.18_175)] to-[oklch(0.55_0.16_160)] bg-clip-text text-transparent">
              {t("heroTitle2")}
            </span>
          </h1>
          <p className="dz-enter-2 mx-auto mt-5 max-w-xl text-pretty text-base text-muted-foreground sm:text-lg">
            {t("heroDescription")}
          </p>
        </section>

        <HeroInput onSubmit={handleSubmit} />

        <section className="dz-enter-5 mx-auto max-w-4xl px-4 pt-4 pb-12 text-center">
          <h3 className="font-display text-lg font-semibold tracking-tight sm:text-xl">
            {t("whatYouGet")}
          </h3>

          <div className="mx-auto mt-4 flex flex-wrap justify-center gap-2">
            {FEATURE_KEYS.map((key, i) => {
              const Icon = FEATURE_ICONS[i];
              return (
                <span
                  key={key}
                  className="inline-flex items-center gap-1.5 rounded-full border border-border/60 bg-secondary/60 px-3 py-1.5 text-xs font-medium text-secondary-foreground backdrop-blur-sm dark:border-primary/10 dark:bg-white/[0.04]"
                >
                  <Icon className="h-3.5 w-3.5 text-primary" />
                  {t(`featurePills.${key}`)}
                </span>
              );
            })}
          </div>

          <div
            className="mx-auto mt-8 hidden max-w-sm sm:block"
            style={{ perspective: "1200px" }}
          >
            <div className="dz-preview-tilt relative overflow-hidden rounded-2xl border border-border/30 bg-card/70 p-4 shadow-xl backdrop-blur-lg dark:border-primary/[0.06] dark:bg-white/[0.03]">
              <div className="mb-3 flex items-center gap-2">
                <div className="h-2.5 w-2.5 rounded-full bg-primary/60" />
                <div className="h-1.5 w-16 rounded-full bg-muted-foreground/20" />
                <div className="ml-auto h-1.5 w-10 rounded-full bg-muted-foreground/15" />
              </div>
              <div className="grid grid-cols-4 gap-1.5 text-left">
                <div className="col-span-2 row-span-2 rounded-lg bg-primary/10 p-2.5">
                  <div className="text-[10px] text-muted-foreground">{t("preview.viability")}</div>
                  <div className="mt-1 text-xl font-bold text-primary">72%</div>
                  <div className="mt-1.5 h-1 w-full rounded-full bg-primary/20">
                    <div className="h-full w-[72%] rounded-full bg-primary" />
                  </div>
                </div>
                <div className="col-span-2 rounded-lg bg-[oklch(0.55_0.18_270_/_0.08)] p-2 dark:bg-[oklch(0.6_0.18_270_/_0.08)]">
                  <div className="text-[9px] text-muted-foreground">{t("preview.competition")}</div>
                  <div className="mt-0.5 text-xs font-semibold">{t("preview.rivals")}</div>
                </div>
                <div className="col-span-2 rounded-lg bg-[oklch(0.6_0.12_160_/_0.08)] p-2 dark:bg-[oklch(0.65_0.12_160_/_0.08)]">
                  <div className="text-[9px] text-muted-foreground">{t("preview.clients")}</div>
                  <div className="mt-0.5 text-xs font-semibold">{t("preview.segments")}</div>
                </div>
                <div className="col-span-1 rounded-lg bg-[oklch(0.6_0.15_80_/_0.08)] p-1.5 dark:bg-[oklch(0.7_0.15_80_/_0.08)]">
                  <div className="text-[8px] text-muted-foreground">{t("preview.legal")}</div>
                </div>
                <div className="col-span-1 rounded-lg bg-primary/5 p-1.5">
                  <div className="text-[8px] text-muted-foreground">{t("preview.kit")}</div>
                </div>
                <div className="col-span-2 rounded-lg bg-[oklch(0.55_0.18_30_/_0.08)] p-1.5 dark:bg-[oklch(0.6_0.18_30_/_0.08)]">
                  <div className="text-[8px] text-muted-foreground">{t("preview.roadmap")}</div>
                  <div className="mt-0.5 flex gap-0.5">
                    <div className="h-0.5 flex-1 rounded-full bg-foreground/20" />
                    <div className="h-0.5 flex-1 rounded-full bg-foreground/10" />
                    <div className="h-0.5 flex-1 rounded-full bg-foreground/5" />
                  </div>
                </div>
              </div>
              <div className="pointer-events-none absolute inset-x-0 bottom-0 h-10 rounded-b-2xl bg-gradient-to-t from-card to-transparent dark:from-[oklch(0.12_0.02_220)]" />
            </div>
            <p className="mt-3 text-xs text-muted-foreground">
              {t("previewCaption")}
            </p>
          </div>
        </section>

        <section className="dz-enter-5 mx-auto max-w-4xl px-4 pb-16 pt-8">
          <h3 className="text-center font-display text-2xl font-bold tracking-tight sm:text-3xl">
            {t("whyDayZero")}
          </h3>
          <div className="mt-8 grid gap-6 sm:grid-cols-3">
            {TRUST_KEYS.map((key, i) => {
              const Icon = TRUST_ICONS[i];
              return (
                <div
                  key={key}
                  className="rounded-2xl border border-border/50 bg-card/70 p-6 backdrop-blur-sm transition-shadow hover:shadow-lg dark:border-primary/[0.06] dark:bg-white/[0.03] dark:hover:shadow-primary/[0.05]"
                >
                  <div className="mb-3 flex h-10 w-10 items-center justify-center rounded-xl bg-primary/10">
                    <Icon className="h-5 w-5 text-primary" />
                  </div>
                  <h4 className="font-display text-base font-semibold">
                    {t(`trustCards.${key}Title`)}
                  </h4>
                  <p className="mt-1.5 text-sm leading-relaxed text-muted-foreground">
                    {t(`trustCards.${key}Desc`)}
                  </p>
                </div>
              );
            })}
          </div>
          <p className="mt-10 text-center text-sm text-muted-foreground">
            {t("hackathonBadge")}
          </p>
        </section>

        <footer className="border-t border-border/40 bg-background/50 backdrop-blur-sm">
          <div className="mx-auto max-w-4xl px-4 py-8 text-center">
            <p className="font-display text-sm font-semibold tracking-tight">
              {t("footerTagline")}
            </p>
            <p className="mt-1 text-xs text-muted-foreground/60">
              {t("footerSubtext")}
            </p>
          </div>
        </footer>
      </div>
    </main>
  );
}
```

- [ ] **Step 3: Verify in browser**

Visit `http://localhost:3000` — all text should be in Spanish.
Visit `http://localhost:3000/en` — all text should be in English.
Toggle the language button — should switch between both.

- [ ] **Step 4: Commit**

```bash
git add components/hero-input.tsx app/\[locale\]/page.tsx
git commit -m "feat(i18n): wire translations into home page and hero input"
```

---

## Task 7: Wire translations into questions and analysis pages

**Files:**
- Modify: `app/[locale]/preguntas-claude/page.tsx`
- Modify: `app/[locale]/analisis/page.tsx`

- [ ] **Step 1: Modify `app/[locale]/preguntas-claude/page.tsx`**

At the top of the file, add imports and hook:

```tsx
import { useTranslations, useLocale } from "next-intl";
import { useRouter } from "@/i18n/navigation";
```

Remove import of `useRouter` from `next/navigation`.

Inside the component, add at the top:

```tsx
const t = useTranslations("questions");
const tCommon = useTranslations("common");
const locale = useLocale();
```

Replace the `loadingMessages` constant array with:

```tsx
const loadingMessages = [
  t("loadingMessages.0"),
  t("loadingMessages.1"),
  t("loadingMessages.2"),
];
```

Then replace every hardcoded Spanish string with the corresponding `t()` call. Examples:
- `"Calibrando tu misión"` → `t("calibrating")`
- `"Esto solo tarda unos segundos"` → `t("calibratingHint")`
- `"Algo salió mal al cargar las preguntas"` → `t("loadError")`
- `"Volver al inicio"` → `tCommon("backToStart")`
- `"No existe una sesión activa..."` → `t("noSession")`
- `"Siguiente"` → `t("nextButton")`
- `"Continuar al análisis"` → `t("continueToAnalysis")`
- `"No se pudo continuar"` → `t("continueError")`
- `"Contanos más 💬"` → `t("detailsPlaceholder")`
- `"Escribí todo el contexto..."` → `t("detailsTextarea")`
- `router.push("/")` → `router.push("/")`  (no change needed — `@/i18n/navigation` handles prefix)

Replace the page indicator pattern:
```tsx
// Before:
`Página ${currentPage + 1} de ${totalPages}`
// After:
t("pageIndicator", { currentPage: currentPage + 1, totalPages })
```

- [ ] **Step 2: Modify `app/[locale]/analisis/page.tsx`**

Same pattern. Add imports:

```tsx
import { useTranslations, useLocale } from "next-intl";
import { useRouter } from "@/i18n/navigation";
```

Inside the component:

```tsx
const t = useTranslations("analysis");
const tCommon = useTranslations("common");
const locale = useLocale();
```

Replace `planningMessages` and `loadingMessages` arrays with `t()` calls.

Replace all hardcoded strings:
- `"Preparando el análisis"` → `t("preparing")`
- `"Generando tu análisis"` → `t("generating")`
- `"No hay una sesión activa..."` → `t("noSession")`
- `"La sesión de análisis no existe..."` → `t("sessionExpired")`
- `"No se pudo generar..."` → `t("backendError")`
- `"Error al construir el análisis"` → `t("errorTitle")`
- `"Volver al inicio"` → `tCommon("backToStart")`

- [ ] **Step 3: Verify in browser**

Navigate through the full flow in both `/` and `/en` — forms should submit, questions should load, and all static text should be translated.

- [ ] **Step 4: Commit**

```bash
git add app/\[locale\]/preguntas-claude/page.tsx app/\[locale\]/analisis/page.tsx
git commit -m "feat(i18n): wire translations into questions and analysis pages"
```

---

## Task 8: Wire translations into login page

**Files:**
- Modify: `app/[locale]/login/page.tsx`

- [ ] **Step 1: Modify `app/[locale]/login/page.tsx`**

Add imports:

```tsx
import { useTranslations } from "next-intl";
import { useRouter } from "@/i18n/navigation";
```

Remove `useRouter` from `next/navigation`.

Inside the component:

```tsx
const t = useTranslations("login");
```

Replace hardcoded strings:
- `"Iniciar sesión"` → `t("title")`
- `"Ingresá tus credenciales para continuar"` → `t("subtitle")`
- `"Contraseña"` → `t("passwordLabel")`
- `"Ocultar contraseña"` / `"Mostrar contraseña"` → `t("hidePassword")` / `t("showPassword")`
- `"Ingresar"` → `t("submitButton")`
- `"Error al iniciar sesión."` → `t("loginError")`
- `"No se pudo conectar con el servidor."` → `t("connectionError")`

- [ ] **Step 2: Commit**

```bash
git add app/\[locale\]/login/page.tsx
git commit -m "feat(i18n): wire translations into login page"
```

---

## Task 9: Wire translations into dashboard components (batch 1: viability, competitors, clients, monetization)

**Files:**
- Modify: `components/dashboard/viability-section.tsx`
- Modify: `components/dashboard/competitors-section.tsx`
- Modify: `components/dashboard/clients-section.tsx`
- Modify: `components/dashboard/monetization-section.tsx`

For each file, the pattern is identical:

1. Add `import { useTranslations, useLocale } from "next-intl";` at the top
2. Add `const t = useTranslations("<namespace>");` inside the component
3. Replace every hardcoded Spanish string with `t("key")` using the keys from the JSON files
4. Replace `"es-AR"` in `toLocaleString` / `Intl.NumberFormat` calls with a locale-aware value: `locale === "es" ? "es-AR" : "en-US"`

- [ ] **Step 1: Modify `viability-section.tsx`**

Add `const t = useTranslations("viabilitySection");` and replace all strings per the `viabilitySection` namespace in the JSON.

- [ ] **Step 2: Modify `competitors-section.tsx`**

Add `const t = useTranslations("competitorsSection");`. Use `t("count", { count: data.competitors.length, city: city ? ` en ${city}` : "" })` for interpolated strings. Note: the city suffix in the competitor count should also be translated — add `" in {city}"` to `en.json` and `" en {city}"` to `es.json` via the `city` parameter.

- [ ] **Step 3: Modify `clients-section.tsx`**

Add `const t = useTranslations("clientsSection");`. The email/message template functions (`buildB2BEmail`, `buildB2CMessage`) should also receive locale and use translated templates. Add `clientsSection.emailSubject`, `clientsSection.emailBody`, `clientsSection.campaignBody` keys to both JSON files for these templates.

- [ ] **Step 4: Modify `monetization-section.tsx`**

Add `const t = useTranslations("monetizationSection");` and `const locale = useLocale();`. Update the `Intl.NumberFormat("es-AR", ...)` call to use `locale === "es" ? "es-AR" : "en-US"`.

- [ ] **Step 5: Verify all four sections render correctly in both languages**

- [ ] **Step 6: Commit**

```bash
git add components/dashboard/viability-section.tsx components/dashboard/competitors-section.tsx components/dashboard/clients-section.tsx components/dashboard/monetization-section.tsx
git commit -m "feat(i18n): wire translations into dashboard batch 1 (viability, competitors, clients, monetization)"
```

---

## Task 10: Wire translations into dashboard components (batch 2: legal, kit, roadmap, obstacles, financial, landing)

**Files:**
- Modify: `components/dashboard/legal-section.tsx`
- Modify: `components/dashboard/kit-section.tsx`
- Modify: `components/dashboard/roadmap-section.tsx`
- Modify: `components/dashboard/obstacles-section.tsx`
- Modify: `components/dashboard/financial-projection-section.tsx`
- Modify: `components/dashboard/landing-cta.tsx`

Same pattern as Task 9 for each file.

- [ ] **Step 1: Modify `legal-section.tsx`**

Add `const t = useTranslations("legalSection");` and `const locale = useLocale();`. Update `toLocaleString("es-AR")` in `formatArs()` to use locale. Replace all Spanish strings.

- [ ] **Step 2: Modify `kit-section.tsx`**

Add `const t = useTranslations("kitSection");` and `const locale = useLocale();`. Update `toLocaleString("es-AR")` calls. Replace all Spanish strings. Use `t("subtitle", { investment: investment.toLocaleString(locale === "es" ? "es-AR" : "en-US") })` for the formatted investment string.

- [ ] **Step 3: Modify `roadmap-section.tsx`**

Add `const t = useTranslations("roadmapSection");`. Replace all Spanish strings.

- [ ] **Step 4: Modify `obstacles-section.tsx`**

Add `const t = useTranslations("obstaclesSection");`. Replace all Spanish strings. Use `t("failureCase", { startup: obstacle.failureCase.startup })` for interpolation.

- [ ] **Step 5: Modify `financial-projection-section.tsx`**

Add `const t = useTranslations("financialSection");` and `const locale = useLocale();`. Update both `Intl.NumberFormat("es-AR", ...)` calls. Replace the `CATEGORIAS_GASTO` and `CATEGORIAS_INGRESO` arrays with `t()` calls. Replace `ESTADO_LABELS` with `t()` calls. Replace all default expense/income names. This is the largest single file — take care to replace every string.

- [ ] **Step 6: Modify `landing-cta.tsx`**

Add `const t = useTranslations("landingCta");`. Replace the `features` array labels and all other strings.

- [ ] **Step 7: Verify all dashboard sections in browser (both languages)**

- [ ] **Step 8: Commit**

```bash
git add components/dashboard/legal-section.tsx components/dashboard/kit-section.tsx components/dashboard/roadmap-section.tsx components/dashboard/obstacles-section.tsx components/dashboard/financial-projection-section.tsx components/dashboard/landing-cta.tsx
git commit -m "feat(i18n): wire translations into dashboard batch 2 (legal, kit, roadmap, obstacles, financial, landing)"
```

---

## Task 11: Wire translations into startup-dashboard.tsx

**Files:**
- Modify: `components/startup-dashboard.tsx`

- [ ] **Step 1: Modify `components/startup-dashboard.tsx`**

Add imports:

```tsx
import { useTranslations, useLocale } from "next-intl";
import { useRouter } from "@/i18n/navigation";
```

Remove `useRouter` from `next/navigation`.

Inside the component:

```tsx
const t = useTranslations("dashboard");
const locale = useLocale();
```

Replace:
- `formatInvestment` to use locale-aware formatting: `(amount: number) => \`$${amount.toLocaleString(locale === "es" ? "es-AR" : "en-US")}\``
- `getVerdictSentence()` to use `t("verdicts.high")`, `t("verdicts.medium")`, `t("verdicts.low")`
- `getCompetitionLabel()` to use `t("competitionLabels.low")`, etc.
- `getTrendLabel()` to use `t("trendLabels.growing")`, etc.
- `"Volver al panel"` → `t("backToPanel")`
- `"Analizando en tiempo real..."` → `t("analyzingRealTime")`
- `"Potencial"` → `t("potential")`
- `"Viabilidad"` → `t("viabilityLabel")`
- `"/10"` → `t("score")`
- All other hardcoded Spanish labels in the summary cards

Also replace `router.push("/")` with the `@/i18n/navigation` router (already imported).

- [ ] **Step 2: Verify the full dashboard renders in both languages**

- [ ] **Step 3: Commit**

```bash
git add components/startup-dashboard.tsx
git commit -m "feat(i18n): wire translations into startup dashboard"
```

---

## Task 12: Pass locale through API layer

**Files:**
- Modify: `lib/flow-api.ts`
- Modify: `lib/server/flow-store.ts`
- Modify: `app/api/flow/route.ts`
- Modify: `app/api/questions/route.ts`
- Modify: `app/api/analysis/route.ts`
- Modify: `app/api/section-plan/route.ts`

- [ ] **Step 1: Modify `lib/flow-api.ts` to pass `X-Locale` header**

Update the `requestJson` helper to accept an optional locale parameter:

```ts
async function requestJson<T>(
  input: RequestInfo | URL,
  init?: RequestInit & { locale?: string }
): Promise<T> {
  const { locale, ...fetchInit } = init ?? {};
  const response = await fetch(input, {
    ...fetchInit,
    headers: {
      "Content-Type": "application/json",
      ...(locale ? { "X-Locale": locale } : {}),
      ...(fetchInit?.headers ?? {}),
    },
  });
  // ... rest unchanged
```

Then update each exported function to accept and pass `locale`:

- `createFlowSession(businessInput, locale?)` — pass `{ locale }` to requestJson
- `saveFlowAnswers(flowId, answers, businessInput?, locale?)` — pass locale
- `getFlowSession(flowId, locale?)` — pass locale
- `fetchSectionPlan(businessInput, answers, locale?)` — pass locale
- `getClarificationQuestionsFromApi(businessInput, flowId?, locale?)` — pass locale
- `streamAnalysis(businessInput, answers, sectionPlan?, locale?)` — add `X-Locale` header to the fetch call

- [ ] **Step 2: Modify `lib/server/flow-store.ts` to add locale field**

Add `locale?: string` to the `FlowRecord` interface:

```ts
export interface FlowRecord {
  id: string;
  businessInput: BusinessInputData;
  claudeAnswers: ClaudeAnswer[];
  locale?: string;
  createdAt: string;
  updatedAt: string;
}
```

Update `createFlow` to accept and store locale:

```ts
export function createFlow(businessInput: BusinessInputData, locale?: string): FlowRecord {
  const now = new Date().toISOString();
  const record: FlowRecord = {
    id: randomUUID(),
    businessInput,
    claudeAnswers: [],
    locale,
    createdAt: now,
    updatedAt: now,
  };
  flowStore.set(record.id, record);
  return record;
}
```

- [ ] **Step 3: Modify `app/api/flow/route.ts` to read and store locale**

In the POST handler, read the locale from the request header:

```ts
const locale = request.headers.get("X-Locale") ?? "es";
```

Pass it to `createFlow(input, locale)`.

- [ ] **Step 4: Modify `app/api/questions/route.ts` to pass locale to prompt builder**

Read the header:

```ts
const locale = request.headers.get("X-Locale") ?? "es";
```

Pass it to `generateQuestionsWithClaude(input, locale)`, which in turn passes it to the prompt builder (Task 13).

- [ ] **Step 5: Modify `app/api/analysis/route.ts` to pass locale to generators**

Read the header:

```ts
const locale = request.headers.get("X-Locale") ?? "es";
```

Pass it to `generateViabilitySection`, `generateDetailsSection`, `generateResearchSection`.

- [ ] **Step 6: Modify `app/api/section-plan/route.ts` to pass locale to prompt builder**

Read the header and pass to `buildSectionPlanUserMessage`.

- [ ] **Step 7: Commit**

```bash
git add lib/flow-api.ts lib/server/flow-store.ts app/api/flow/route.ts app/api/questions/route.ts app/api/analysis/route.ts app/api/section-plan/route.ts
git commit -m "feat(i18n): pass locale through API layer via X-Locale header"
```

---

## Task 13: Localize AI system prompts

**Files:**
- Modify: `lib/server/questions-prompt.ts`
- Modify: `lib/server/section-plan-prompt.ts`

- [ ] **Step 1: Modify `lib/server/questions-prompt.ts`**

The current `QUESTIONS_SYSTEM_PROMPT` is a single static string in Spanish. For prompt caching to work, we need the static portion to stay cacheable. The approach: create two static prompts (one per language) and select at call time.

```ts
export const QUESTIONS_SYSTEM_PROMPT_ES = `Sos un asesor de startups experto en el mercado argentino (2026).
...existing Spanish prompt unchanged...`;

export const QUESTIONS_SYSTEM_PROMPT_EN = `You are an expert startup advisor specializing in the Argentine market (2026).

Your task is to analyze a business idea and determine if you need additional CRITICAL information to generate a high-quality analysis. Only ask what is essential.

PHILOSOPHY: Always prefer to infer rather than ask. Only ask a question when:
a) The answer is truly ambiguous (you cannot infer with 70%+ confidence), AND
b) The answer would radically change the analysis in at least one section

... (full English translation of the prompt) ...

FORMAT: Return ONLY a valid JSON array. No text before or after.`;

export function getQuestionsSystemPrompt(locale: string): string {
  return locale === "en" ? QUESTIONS_SYSTEM_PROMPT_EN : QUESTIONS_SYSTEM_PROMPT_ES;
}
```

Update `buildQuestionsUserMessage` to accept locale:

```ts
export function buildQuestionsUserMessage(input: BusinessInputData, locale: string): string {
  if (locale === "en") {
    const lines = [`- Description: ${input.idea}`];
    if (input.city) lines.push(`- City: ${input.city}`);
    if (input.investment) lines.push(`- Initial investment: ARS ${input.investment.toLocaleString("en-US")}`);
    return `BUSINESS IDEA:\n${lines.join("\n")}\n\nAnalyze this idea evaluating what each analysis section needs. Return ONLY the JSON array with the necessary clarification questions, or an empty array [] if you can infer everything with confidence.`;
  }

  // existing Spanish version
  const lines = [`- Descripción: ${input.idea}`];
  if (input.city) lines.push(`- Ciudad: ${input.city}`);
  if (input.investment) lines.push(`- Inversión inicial disponible: ARS ${input.investment.toLocaleString("es-AR")}`);
  return `IDEA DE NEGOCIO:\n${lines.join("\n")}\n\nAnalizá esta idea evaluando qué necesita cada sección del análisis. Devolvé SOLO el array JSON con las preguntas de clarificación necesarias, o un array vacío [] si podés inferir todo con confianza.`;
}
```

- [ ] **Step 2: Modify `lib/server/section-plan-prompt.ts`**

Same pattern — create `SECTION_PLAN_SYSTEM_PROMPT_EN` with an English version of the system prompt, and a `getSectionPlanSystemPrompt(locale)` selector.

Update `buildSectionPlanUserMessage` to accept locale:

```ts
export function buildSectionPlanUserMessage(
  input: BusinessInputData,
  answers: ClaudeAnswer[],
  locale: string
): string {
  if (locale === "en") {
    const lines = [`- Idea: ${input.idea}`];
    if (input.city) lines.push(`- City: ${input.city}`);
    if (input.investment) lines.push(`- Initial investment: ARS ${input.investment.toLocaleString("en-US")}`);
    if (input.brandName) lines.push(`- Brand: ${input.brandName}`);
    if (input.businessType) lines.push(`- Type: ${input.businessType}`);

    const answersText = answers.length > 0
      ? answers.map((a) => `- ${a.questionId}: "${a.option}"${a.details ? ` — ${a.details}` : ""}`).join("\n")
      : "(no additional questions)";

    return `BUSINESS INFORMATION:\n${lines.join("\n")}\n\nFOUNDER ANSWERS:\n${answersText}\n\nEvaluate which analysis sections apply to this idea. Return ONLY the JSON.`;
  }

  // existing Spanish version unchanged
  ...
}
```

- [ ] **Step 3: Update callers to pass locale**

In `app/api/questions/route.ts`, update the Claude call:

```ts
const systemPrompt = getQuestionsSystemPrompt(locale);
// Use systemPrompt instead of QUESTIONS_SYSTEM_PROMPT in the API call
```

In `app/api/section-plan/route.ts`, update similarly:

```ts
const systemPrompt = getSectionPlanSystemPrompt(locale);
// Use systemPrompt instead of SECTION_PLAN_SYSTEM_PROMPT
```

- [ ] **Step 4: Commit**

```bash
git add lib/server/questions-prompt.ts lib/server/section-plan-prompt.ts app/api/questions/route.ts app/api/section-plan/route.ts
git commit -m "feat(i18n): localize AI system prompts for questions and section plan"
```

---

## Task 14: Localize analysis prompt

**Files:**
- Modify: `lib/server/analysis-prompt.ts`

- [ ] **Step 1: Create English version of the analysis system prompt**

Same pattern as Task 13. `lib/server/analysis-prompt.ts` contains a large Spanish system prompt (`ANALYSIS_SYSTEM_PROMPT`) and a `buildUserMessage()` function that produces Spanish text.

Create `ANALYSIS_SYSTEM_PROMPT_EN` with a full English translation of the system prompt. Create `getAnalysisSystemPrompt(locale)` selector.

Update `buildUserMessage` to accept `locale: string` parameter:
- English version: uses `"en-US"` for number formatting, English labels for fields ("Description:", "City:", "Initial investment:")
- Spanish version: unchanged (existing behavior)

- [ ] **Step 2: Update callers**

In `lib/server/section-generators.ts` (or wherever `ANALYSIS_SYSTEM_PROMPT` and `buildUserMessage` are called), pass the locale parameter through.

- [ ] **Step 3: Commit**

```bash
git add lib/server/analysis-prompt.ts lib/server/section-generators.ts
git commit -m "feat(i18n): localize analysis system prompt"
```

---

## Task 15: Pass locale from client pages to API calls

**Files:**
- Modify: `app/[locale]/page.tsx`
- Modify: `app/[locale]/preguntas-claude/page.tsx`
- Modify: `app/[locale]/analisis/page.tsx`

- [ ] **Step 1: Update home page to pass locale to `createFlowSession`**

In `app/[locale]/page.tsx`, the `handleSubmit` already has access to locale via `useLocale()`. Add:

```tsx
const locale = useLocale();
```

Update the call:

```tsx
const flowId = await createFlowSession(data, locale);
```

- [ ] **Step 2: Update questions page to pass locale**

In `app/[locale]/preguntas-claude/page.tsx`, pass locale to:
- `getClarificationQuestionsFromApi(businessInput, flowId, locale)`
- `saveFlowAnswers(flowId, answers, businessInput, locale)`

- [ ] **Step 3: Update analysis page to pass locale**

In `app/[locale]/analisis/page.tsx`, pass locale to:
- `getFlowSession(flowId, locale)`
- `fetchSectionPlan(businessInput, answers, locale)`
- `streamAnalysis(businessInput, answers, sectionPlan, locale)`

- [ ] **Step 4: Verify the full flow works end-to-end in English**

1. Visit `/en`
2. Fill in a business idea
3. Answer clarification questions (should be in English)
4. View the analysis (dashboard labels in English, AI-generated content in English)

- [ ] **Step 5: Commit**

```bash
git add app/\[locale\]/page.tsx app/\[locale\]/preguntas-claude/page.tsx app/\[locale\]/analisis/page.tsx
git commit -m "feat(i18n): pass locale from client pages to API calls"
```

---

## Task 16: Final verification and cleanup

- [ ] **Step 1: Run the build**

```bash
pnpm build
```

Fix any TypeScript/build errors.

- [ ] **Step 2: Run lint**

```bash
pnpm lint
```

Fix any lint errors.

- [ ] **Step 3: End-to-end manual verification**

Test in browser:
1. **Spanish flow (default):** Visit `/` → fill form → submit → answer questions → view analysis → all Spanish
2. **English flow:** Visit `/en` → fill form → submit → answer questions → view analysis → all English
3. **Toggle mid-flow:** Start in Spanish, toggle to English on the questions page → UI switches, flow continues
4. **Direct URL:** Visit `/en/analisis` directly without a session → should redirect to `/en` with error
5. **Cookie persistence:** Toggle to English, close browser, reopen → should stay on English

- [ ] **Step 4: Remove any leftover hardcoded Spanish strings**

Search the codebase for any remaining hardcoded Spanish:

```bash
pnpm build 2>&1 | head -50
```

Grep for common Spanish patterns that shouldn't be in component files:

```bash
grep -r "\"Ej:" components/ app/\[locale\]/ --include="*.tsx" | grep -v node_modules
```

If any found, extract them into the translation files.

- [ ] **Step 5: Final commit**

```bash
git add -A
git commit -m "feat(i18n): final cleanup and verification"
```

---

**Note on `lib/mock-data.ts`:** The spec mentions adding parallel English string templates to `generateAnalysis()`. In practice, mock data is only used as seed/fallback — the real analysis content comes from Claude via locale-aware prompts (Tasks 13-14). If the mock data fallback path is exercised (e.g., when Claude API is unreachable), it will still return Spanish strings. This is acceptable for now — adding full English templates to mock-data.ts can be a follow-up if needed, since it's a large file (~500 lines) with many embedded Spanish strings that would double in size.
